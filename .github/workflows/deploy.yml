name: Deploy Frontend para Hostinger

on:
  push:
    branches:
      - main
      - master

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Instalar dependências
        run: npm install

      - name: Build Next.js
        run: npm run build

      - name: Preparar arquivos para deploy
        run: |
          mkdir -p deploy/.next
          cp .next/standalone/server.js deploy/server.js
          cp -r .next/standalone/node_modules deploy/node_modules
          cp -r .next/standalone/.next/. deploy/.next/
          cp -r .next/static deploy/.next/static
          cp -r public deploy/public

      - name: Upload para servidor via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ secrets.FTP_HOST }}
          username: ${{ secrets.FTP_USER }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: ./deploy/
          server-dir: domains/razepag.com/nodejs/
          dangerous-clean-slate: true
          timeout: 600000
